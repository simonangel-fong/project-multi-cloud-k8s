apiVersion: v1
kind: Service
metadata:
  name: {{ include "multicloud-demo-api.fullname" . }}
  labels:
    {{- include "multicloud-demo-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multicloud-demo-api.selectorLabels" . | nindent 4 }}
